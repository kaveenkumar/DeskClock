<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Black window background so the moment before Compose draws is invisible on the OLED panel. -->
    <style name="Theme.DeskClock" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
